---
// src/components/FinalCTASection.astro
---
{/* Añadimos un div wrapper para aplicar fondo y padding */}
<div class="final-cta-wrapper section-padding">
    {/* Usamos el .container global aquí dentro */}
    <div class="container">
      <div class="cta-content">
          <h2 class="cta-title">Ready to Transform Your Business with AI?</h2>
          <p class="cta-subtitle">
            Stop letting manual processes slow you down. Let's discuss how intelligent
            automation can drive efficiency and growth for you.
          </p>
          <a href="/contact" class="cta-button-large">Schedule Your Free Consultation</a>
      </div>
    </div>
  </div>
  
  <style>
      /* --- Estilos para el Wrapper Principal --- */
      .final-cta-wrapper {
          background-color: var(--primary-color); /* Fondo de color primario */
          padding-bottom: 0rem; /* Un poco más abajo para el botón */
          color: #fff; /* Color de texto por defecto para esta sección */
      }
  
  
      /* --- Estilos para el Contenido Interno --- */
      .cta-content {
          text-align: center; /* <<< CENTRA TODO EL CONTENIDO INTERNO */
      }
  
      .cta-title {
          font-size: clamp(1.8rem, 4vw, 2.5rem);
          color: #fff; /* Asegura blanco */
          margin-top: 0;
          margin-bottom: 1rem;
          font-weight: 700;
          line-height: 1.3;
      }
  
      .cta-subtitle {
          font-size: 1.1rem;
          color: rgba(255, 255, 255, 0.9); /* Un poco más opaco */
          max-width: 600px;
          margin: 0 auto 2.5rem auto;
          line-height: 1.6;
      }
  
      .cta-button-large {
        display: inline-block;
        background-color: #fff;
        color: var(--primary-color);
        padding: 1rem 2.5rem;
        border-radius: 8px;
        text-decoration: none;
        font-weight: bold;
        font-size: 1.1rem;
        transition: transform 0.2s ease, background-color 0.2s ease, color 0.2s ease;
        border: 2px solid transparent;
        cursor: pointer;
        margin-top: 1rem; /* Espacio sobre el botón  */
      }
  
      .cta-button-large:hover {
        background-color: transparent;
        color: #fff;
        border-color: #fff;
        transform: scale(1.03);
      }
  

      :global(.section-padding) {
    /* Asegura que exista o defínelo globalmente */
    padding-top: 0rem;
    padding-bottom: 0rem;
}
  
  </style>