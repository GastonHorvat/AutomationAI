---
// src/pages/approach.astro
import Layout from '../layouts/Layout.astro';
import { Icon } from 'astro-icon/components';
// Importa cualquier imagen específica si la necesitas (ej. para fondos sutiles)
// import subtleBg from '../assets/images/subtle-background.png';

// Puedes definir los pasos y principios como arrays si prefieres más separación
// pero por ahora los dejamos directo en el HTML por claridad con el contenido dado.
---

<Layout title="Our Approach | AutomationAI Solutions" description="Discover AutomationAI's collaborative, results-driven approach to implementing AI solutions that deliver tangible impact and rapid ROI for your business.">

  {/* ====================================================================== */}
  {/* --- Sección 1: Hero / Filosofía --- */}
  {/* ====================================================================== */}
  <section class="approach-hero section-padding bg-soft-gradient text-center">
    <div class="container narrow-container">
      <h1 class="approach-h1">Our Approach: From Insight to Impact</h1>
      <p class="page-subtitle large-subtitle approach-subtitle">
        We believe implementing AI shouldn't be complex or intimidating. It should be a strategic partnership focused on tangible results.
      </p>
      <p class="approach-intro">
        Our entire approach is built on collaboration, deep analysis, and a practical mindset. We don't just implement technology; we partner with you to understand your unique business context and deliver AI solutions that provide clear, measurable value and a rapid return on investment.
      </p>
    </div>
  </section>

  {/* ====================================================================== */}
  {/* --- Sección 2: El Proceso Detallado --- */}
  {/* ====================================================================== */}
  <section class="process-section section-padding">
    <div class="container">
      <h2 class="section-title-alt text-center">Our Collaborative Process: Your AI Journey, Step-by-Step</h2>
      <p class="section-intro text-center">
        We follow a structured yet flexible methodology to ensure clarity, alignment, and successful outcomes.
      </p>

      <div class="process-steps-grid">
        {/* Paso 1 */}
        <div class="process-step-card">
          <div class="step-number">1</div>
          <div class="step-icon">
            <Icon name="mdi:magnify" size={48} />
          </div>
          <h3 class="step-title">Deep Dive Discovery</h3>
          <p class="step-text">We begin by immersing ourselves in your world. We meticulously analyze your current workflows, tools, pain points, and strategic objectives. Through active listening and targeted questions, we pinpoint the highest-impact opportunities where AI can make a real difference.</p>
        </div>

        {/* Paso 2 */}
        <div class="process-step-card">
           <div class="step-number">2</div>
          <div class="step-icon">
            <Icon name="mdi:compass-rose" size={48} /> {/* Cambiado a compass-rose */}
          </div>
          <h3 class="step-title">Strategic Solution Design</h3>
          <p class="step-text">Leveraging insights from the discovery phase, we craft a bespoke AI strategy. This isn't a generic template; it's a clear, actionable roadmap outlining the 'what, why, and how' of the proposed solution, always prioritizing practical application and rapid ROI.</p>
        </div>

        {/* Paso 3 */}
        <div class="process-step-card">
           <div class="step-number">3</div>
          <div class="step-icon">
            <Icon name="mdi:cogs" size={48} />
          </div>
          <h3 class="step-title">Seamless Implementation & Integration</h3>
          <p class="step-text">Our experienced team gets to work, building and integrating the AI solution with precision. We focus on a smooth transition, ensuring the technology integrates seamlessly into your existing systems and workflows with minimal disruption to your daily operations.</p>
        </div>

        {/* Paso 4 */}
        <div class="process-step-card">
          <div class="step-number">4</div>
          <div class="step-icon">
            <Icon name="mdi:chart-line" size={48} />
          </div>
          <h3 class="step-title">Ongoing Support & Growth Partnership</h3>
          <p class="step-text">Launch isn't the end; it's the beginning of optimization. We provide dedicated support, monitor performance against key metrics, and proactively collaborate with you to refine the solution and identify new opportunities for continuous AI-driven improvement.</p>
        </div>
      </div>
    </div>
  </section>

  {/* ====================================================================== */}
  {/* --- Sección 3: Principios Fundamentales --- */}
  {/* ====================================================================== */}
  <section class="principles-section section-padding bg-light-gray">
    <div class="container">
      <h2 class="section-title-alt text-center">Principles That Guide Us</h2>
      <p class="section-intro text-center">
        Our process is powered by core beliefs that ensure we deliver the right solutions, the right way.
      </p>

      <div class="principles-grid">
        {/* Principio 1 */}
        <div class="principle-item">
          <div class="principle-icon">
            <Icon name="mdi:puzzle-edit-outline" size={40} />
          </div>
          <h3 class="principle-title">Tailored, Not Templated</h3>
          <p class="principle-text">We fundamentally reject one-size-fits-all. Your business is unique, and your AI solutions should be too. Every strategy and implementation is meticulously crafted for your specific context.</p>
        </div>

        {/* Principio 2 */}
        <div class="principle-item">
          <div class="principle-icon">
            <Icon name="mdi:target-arrow" size={40} />
          </div>
          <h3 class="principle-title">Practicality & ROI First</h3>
          <p class="principle-text">We focus on implementing AI that delivers tangible, measurable results and a clear return on investment quickly. We cut through the hype to provide solutions with real-world value.</p>
        </div>

        {/* Principio 3 */}
        <div class="principle-item">
          <div class="principle-icon">
            <Icon name="mdi:handshake-outline" size={40} />
          </div>
          <h3 class="principle-title">Transparent Collaboration</h3>
          <p class="principle-text">We believe in building true partnerships through open, honest communication. You'll be involved and informed at every stage, ensuring alignment and shared success.</p>
        </div>
      </div>
    </div>
  </section>

  {/* ====================================================================== */}
  {/* --- Sección 4: Filosofía Tecnológica --- */}
  {/* ====================================================================== */}
  <section class="tech-stance-section section-padding">
    <div class="container narrow-container">
       <h2 class="section-title-alt text-center">Our Technology Stance</h2>
       <blockquote class="tech-stance-quote">
         <p>We are technology-agnostic. Our priority is selecting the right AI tools, platforms, and algorithms – whether established or emerging – that best solve your specific problem within your constraints. The optimal outcome dictates the technology, not the other way around.</p>
       </blockquote>
    </div>
  </section>

  {/* ====================================================================== */}
  {/* --- Sección 5: Llamada a la Acción Final --- */}
  {/* ====================================================================== */}
  <section class="final-cta-section section-padding bg-gradient"> {/* Reutiliza .bg-gradient o crea uno específico */}
     <div class="container text-center narrow-container">
        <h2>Let's Build Your <span class="highlight">AI Advantage</span>, Together.</h2>
        <p>Ready to see how our tailored approach can transform your specific business challenges into growth opportunities?</p>
        <div class="cta-buttons">
          <a href="/contact" class="cta-button-primary">Contact Us for a Consultation</a>
          <a href="/solutions" class="cta-button-secondary">Explore Our Solutions</a>
        </div>
     </div>
  </section>

</Layout>

{/* ====================================================================== */}
{/* --- ESTILOS CSS --- */}
{/* ====================================================================== */}
<style>
  /* --- Variables y Globales (Asegúrate que coincidan con tu sitio) --- */
  :global(body) {
     --primary-color: #007bff; /* Tu azul */
     --text-dark: #1A2B48;
     --text-medium: #4A5568;
     --text-light: #6c757d;
     --bg-light-gray: #f8f9fa;
     --border-color-soft: #e2e8f0;
     --white: #ffffff;
     --soft-gradient-start: #f8faff; /* Ejemplo para hero */
     --soft-gradient-end: #ffffff;
  }
  :global(.section-padding) { padding-top: 6rem; padding-bottom: 6rem; } /* Más padding */
  :global(.container) { max-width: 1100px; margin: 0 auto; padding: 0 1.5rem; }
  :global(.narrow-container) { max-width: 800px; }
  :global(.text-center) { text-align: center; }
  :global(.highlight) { color: var(--primary-color); font-weight: 600; }
  :global(.bg-light-gray) { background-color: var(--bg-light-gray); }
  :global(.bg-gradient) { /* Gradiente para CTA final - AJUSTA */
      background: linear-gradient(135deg, rgba(235, 245, 255, 0.8) 0%, rgba(245, 250, 255, 1) 100%);
  }

  /* --- Estilos Generales de Texto para esta Página --- */
  h1, h2, h3 { color: var(--text-dark); font-weight: 700; line-height: 1.3; }
  h1.approach-h1 { font-size: clamp(2.8rem, 7vw, 4.5rem); margin-bottom: 1.5rem; } /* H1 más grande */
  h2.section-title-alt {
    font-size: clamp(2rem, 5vw, 3rem); margin-bottom: 1.5rem; /* Título sección */
    position: relative; padding-bottom: 0.75rem;
  }
   /* Línea bajo H2 */
   h2.section-title-alt::after {
    content: ''; display: block; width: 70px; height: 4px;
    background-color: var(--primary-color); border-radius: 2px;
    margin: 1rem auto 0; /* Centrado si .text-center está presente */
   }
   /* Ajuste si H2 no está centrado */
   h2.section-title-alt:not(.text-center)::after { margin-left: 0; }

  p { color: var(--text-medium); line-height: 1.7; }
  .page-subtitle { font-size: 1.25rem; color: var(--text-medium); max-width: 750px; margin: 0 auto 1.5rem auto; line-height: 1.7; }
  .large-subtitle { font-size: clamp(1.2rem, 3vw, 1.5rem); font-weight: 400; } /* Subtítulo hero más grande */
  .section-intro { font-size: 1.1rem; color: var(--text-light); max-width: 700px; margin: 0 auto 4rem auto; } /* Intro bajo H2 */

  /* ====================================================================== */
  /* --- 1. Estilos Hero --- */
  /* ====================================================================== */
  .approach-hero {
    /* Fondo suave distintivo */
    background: linear-gradient(180deg, var(--soft-gradient-start) 0%, var(--soft-gradient-end) 100%);
  }
  .approach-subtitle { margin-bottom: 2rem; }
  .approach-intro { font-size: 1.1rem; color: var(--text-medium); }

  /* ====================================================================== */
  /* --- 2. Estilos Sección Proceso --- */
  /* ====================================================================== */
  .process-section { background-color: var(--white); }

  .process-steps-grid {
    display: grid;
    grid-template-columns: 1fr; /* Mobile: 1 columna */
    gap: 3rem;
    margin-top: 4rem;
    position: relative; /* Para líneas de conexión */
  }

  @media (min-width: 768px) { /* Tablet: 2 columnas */
    .process-steps-grid { grid-template-columns: repeat(2, 1fr); gap: 2.5rem; }
  }
  @media (min-width: 1024px) { /* Desktop: 4 columnas */
    .process-steps-grid { grid-template-columns: repeat(4, 1fr); gap: 2rem; }

    /* --- Líneas de conexión (Opcional, versión simple) --- */
    .process-step-card:not(:last-child)::after {
        content: '';
        position: absolute;
        top: calc(24px + 1.5rem); /* Alineado verticalmente con el icono (tamaño icono/2 + margin-bottom) */
        left: 100%;
        transform: translateY(-50%);
        width: calc(2rem - 2px); /* Ancho del gap entre tarjetas */
        height: 2px;
        background-color: var(--border-color-soft);
        z-index: -1; /* Detrás de las tarjetas */
    }
     /* Ocultar línea en tablet cuando se rompe a 2x2 */
     @media (min-width: 768px) and (max-width: 1023px) {
         .process-step-card:nth-child(2)::after { display: none; }
     }
  }

  .process-step-card {
    background-color: var(--white);
    border: 1px solid var(--border-color-soft);
    border-radius: 12px;
    padding: 2.5rem 2rem;
    text-align: center;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    position: relative; /* Para número y líneas */
    overflow: hidden; /* Para número */
  }
  .process-step-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 15px 30px rgba(0, 50, 100, 0.1);
  }

  .step-number {
      position: absolute;
      top: -10px;
      left: -10px;
      background-color: var(--primary-color);
      color: var(--white);
      width: 40px;
      height: 40px;
      border-radius: 50% 0 12px 0; /* Esquina redondeada */
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.1rem;
      font-weight: 700;
      line-height: 1;
  }

  .step-icon {
    color: var(--primary-color);
    margin-bottom: 1.5rem;
    display: inline-block; /* Para centrar si es necesario */
  }
  .step-icon svg { /* Asegura tamaño si es necesario */
      width: 48px;
      height: 48px;
  }

  .step-title {
    font-size: 1.4rem;
    margin-bottom: 1rem;
    color: var(--text-dark);
    font-weight: 600;
  }
  .step-text {
    font-size: 0.95rem;
    color: var(--text-medium);
    line-height: 1.6;
  }


  /* ====================================================================== */
  /* --- 3. Estilos Sección Principios --- */
  /* ====================================================================== */
  /* .principles-section usa .bg-light-gray global */

  .principles-grid {
    display: grid;
    grid-template-columns: 1fr; /* Mobile: 1 columna */
    gap: 3rem;
    margin-top: 4rem;
  }

  @media (min-width: 768px) { /* Desktop/Tablet: 3 columnas */
    .principles-grid { grid-template-columns: repeat(3, 1fr); gap: 2.5rem; }
  }

  .principle-item {
    text-align: center;
    padding: 1.5rem; /* Menos padding que las cards de proceso */
  }

  .principle-icon {
    color: var(--primary-color);
    margin-bottom: 1.2rem;
    display: inline-block;
    /* Opcional: Fondo sutil para el icono */
    /* background-color: rgba(0, 123, 255, 0.1); */
    /* border-radius: 50%; */
    /* padding: 15px; */
  }
   .principle-icon svg {
       width: 40px; height: 40px;
   }

  .principle-title {
    font-size: 1.3rem;
    margin-bottom: 0.8rem;
    font-weight: 600;
  }
  .principle-text {
    font-size: 0.95rem;
    color: var(--text-medium);
    line-height: 1.6;
  }


  /* ====================================================================== */
  /* --- 4. Estilos Sección Tech Stance --- */
  /* ====================================================================== */
  .tech-stance-section { background-color: var(--white); }
  .tech-stance-quote {
    border-left: 4px solid var(--primary-color);
    margin-top: 2rem;
    padding: 1rem 0 1rem 2rem;
    font-size: 1.15rem;
    font-style: italic;
    color: var(--text-medium);
    line-height: 1.8;
  }
   .tech-stance-quote p { margin-bottom: 0; }


  /* ====================================================================== */
  /* --- 5. Estilos CTA Final --- */
  /* ====================================================================== */
  /* .final-cta-section usa :global(.bg-gradient) */
  .final-cta-section h2 {
      color: var(--text-dark); /* Ajusta si el gradiente es oscuro */
      font-size: clamp(1.8rem, 5vw, 2.5rem);
      margin-bottom: 1rem;
  }
  .final-cta-section p {
      margin-bottom: 2.5rem;
      font-size: 1.15rem;
      color: var(--text-medium); /* Ajusta si el gradiente es oscuro */
  }
  .cta-buttons {
      display: flex; flex-wrap: wrap; justify-content: center; gap: 1.2rem;
  }
  /* Estilos Botones (Asegúrate que coincidan con los de tu sitio) */
  .cta-button-primary, .cta-button-secondary {
      padding: 0.9rem 2.2rem; border-radius: 8px; text-decoration: none;
      font-weight: 600; font-size: 1rem; transition: all 0.2s ease;
      border: 2px solid transparent; cursor: pointer; display: inline-block;
      text-transform: uppercase; letter-spacing: 0.5px;
  }
  .cta-button-primary { background-color: var(--primary-color); color: var(--white); }
  .cta-button-primary:hover { background-color: transparent; color: var(--primary-color); border-color: var(--primary-color); }
  .cta-button-secondary { background-color: #e9ecef; color: var(--text-dark); }
  .cta-button-secondary:hover { background-color: #ced4da; color: var(--text-dark); }

</style>