---
/*
 * PÁGINA DE ENFOQUE/METODOLOGÍA (APPROACH)
 * Esta página presenta la metodología, proceso y principios de la empresa
 * para implementar soluciones de IA, organizada en secciones modulares
 * que muestran el flujo de trabajo, beneficios y valores de la empresa.
 */

// Importación del layout principal que proporciona la estructura común de la página
import Layout from '../layouts/Layout.astro';

// Importación del componente de iconos para utilizar SVGs
import { Icon } from 'astro-icon/components';

// Importación de componentes modulares que conforman las diferentes secciones de la página
import ApproachHero from '../components/approachSection/ApproachHero.astro';  // Hero section
import Process from '../components/approachSection/Process.astro';            // Sección de proceso detallado
import Benefits from '../components/approachSection/Benefits.astro';          // Sección de beneficios
import Principles from '../components/approachSection/Principles.astro';      // Sección de principios
import CTA from '../components/approachSection/FinalCTA.astro';              // Call-to-Action final

---

<!-- 
  ESTRUCTURA PRINCIPAL DE LA PÁGINA
  - Utiliza el componente Layout para heredar la estructura común (header, footer, etc.)
  - Recibe un título y descripción SEO específicos para esta página
-->
<Layout title="Our Approach | AutomationAI Solutions" description="Discover AutomationAI's collaborative, results-driven approach to implementing AI solutions that deliver tangible impact and rapid ROI for your business.">

  <!-- ====================================================================== -->
  <!-- --- Sección 1: Sección Hero --- -->
  <!-- ====================================================================== -->
  <!-- 
    HERO SECTION
    - Componente importado que muestra el banner principal de la página
    - Normalmente incluye título principal, subtítulo y posiblemente una imagen
  -->
  <ApproachHero />

  <!-- ====================================================================== -->
  <!-- --- Sección 2: El Proceso Detallado --- -->
  <!-- ====================================================================== -->
  <!-- 
    SECCIÓN DE PROCESO
    - Componente importado que muestra el proceso paso a paso
    - Típicamente presenta un flujo de trabajo con etapas secuenciales
  -->
  <Process />

  <!-- ====================================================================== -->
  <!-- --- Sección 3: Beneficios --- -->
  <!-- ====================================================================== -->
  <!-- 
    SECCIÓN DE BENEFICIOS
    - Componente importado que muestra los beneficios cuantificables
    - Presenta tarjetas con métricas y resultados esperados
  -->
  <Benefits />

  <!-- ====================================================================== -->
  <!-- --- Sección 4: Principios --- -->
  <!-- ====================================================================== -->
  <!-- 
    SECCIÓN DE PRINCIPIOS
    - Componente importado que muestra los valores fundamentales
    - Presenta los principios que guían la metodología de la empresa
  -->
  <Principles />

  <!-- ====================================================================== -->
  <!-- --- Sección 5: CTA --- -->
  <!-- ====================================================================== -->
  <!-- 
    SECCIÓN DE CALL-TO-ACTION FINAL
    - Componente importado que muestra la llamada a la acción final
    - Invita a los usuarios a contactar o explorar más soluciones
  -->
  <CTA />

</Layout>

<!-- ====================================================================== -->
<!-- --- ESTILOS CSS --- -->
<!-- ====================================================================== -->
<style>
  /**
   * VARIABLES GLOBALES Y ESTILOS BASE
   * Estas variables definen la paleta de colores y estilos base
   * que se utilizan en toda la página y sus componentes
   */
  
  /**
   * Variables de colores y estilos globales
   * - Estas variables están disponibles para todos los componentes de la página
   * - Definen la paleta de colores corporativa y estilos base
   */
  :global(body) {
     --primary-color: #007bff;        /* Color principal azul corporativo */
     --text-dark: #1A2B48;            /* Color para textos principales/títulos */
     --text-medium: #4A5568;          /* Color para textos de cuerpo/párrafos */
     --text-light: #6c757d;           /* Color para textos secundarios/subtítulos */
     --bg-light-gray: #f8f9fa;        /* Color de fondo claro para secciones alternativas */
     --border-color-soft: #e2e8f0;    /* Color sutil para bordes */
     --white: #ffffff;                /* Color blanco puro */
     --soft-gradient-start: #f8faff;  /* Color inicial para gradientes suaves */
     --soft-gradient-end: #ffffff;    /* Color final para gradientes suaves */
  }
  
  /**
   * Clase utilitaria para espaciado estándar de secciones
   * - Aplica padding vertical consistente a todas las secciones
   * - Padding superior menor que el inferior para mejor ritmo visual
   */
  :global(.section-padding) { 
    padding-top: 2rem; 
    padding-bottom: 5rem; 
  }
  
  /**
   * Clase utilitaria para contenedores principales
   * - Limita el ancho máximo del contenido
   * - Centra el contenido horizontalmente
   * - Agrega padding horizontal para evitar que el contenido toque los bordes
   */
  :global(.container) { 
    max-width: 1100px; 
    margin: 0 auto; 
    padding: 0 1.5rem; 
  }
  
  /**
   * Clase utilitaria para contenedores más estrechos
   * - Útil para secciones donde se quiere centralizar más la atención
   * - Típicamente usado en secciones de texto o CTA
   */
  :global(.narrow-container) { 
    max-width: 800px; 
  }
  
  /**
   * Clase utilitaria para centrar texto
   * - Alinea todo el contenido textual al centro
   */
  :global(.text-center) { 
    text-align: center; 
  }
  
  /**
   * Clase utilitaria para resaltar texto
   * - Aplica el color primario de la marca
   * - Añade peso visual con negrita
   */
  :global(.highlight) { 
    color: var(--primary-color); 
    font-weight: 600; 
  }
  
  /**
   * Clase utilitaria para fondos gris claro
   * - Aplica el color de fondo gris claro definido en las variables
   */
  :global(.bg-light-gray) { 
    background-color: var(--bg-light-gray); 
  }
  
  /**
   * Clase utilitaria para fondos con gradiente
   * - Crea un gradiente suave de azul muy claro a blanco
   * - La dirección del gradiente es diagonal (135 grados)
   */
  :global(.bg-gradient) {
    background: linear-gradient(135deg, rgba(235, 245, 255, 0.8) 0%, rgba(245, 250, 255, 1) 100%);
  }
</style>